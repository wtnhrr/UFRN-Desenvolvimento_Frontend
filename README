
- - -
# To-Do Project (Avaliação 1)

Este é um projeto de lista de tarefas simples, desenvolvido para ajudar os usuários a organizar e gerenciar suas atividades diárias. Ele permite adicionar, editar, marcar como concluídas e excluir tarefas.

## Como iniciar o projeto

1. Certifique-se de ter o [Node.js](https://nodejs.org/) instalado em sua máquina.

2. Clone este repositório:
    ```
    git clone <URL_DO_REPOSITORIO>
    ```
3. Navegue até o diretório do projeto:
    ```
    cd To-Do
    ```
4. Instale as dependências:
    ```
    npm install
    ```
5. Inicie o servidor de desenvolvimento:
    ```
    npm start
    ```
6. Abra o navegador e acesse `http://localhost:3000` para visualizar o projeto.
